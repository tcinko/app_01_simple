<% provide(:title, 'Home') %>
<% str = 'For start to use application create a new account (Sign Up) or log in (name5@email.com, 555).
If your want log in with admin privileges, please, contact to web master for
recive ready to account with the appropriate privileges.' %>

<% if signed_in? %>
  <%= render 'static_pages/home_if_signed' %>
<% else %>
  <hr />
    <%= render 'static_pages/home_not_signed' %>
  <hr />
  <b> <%= str %> </b>
<% end %>
